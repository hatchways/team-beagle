import { FetchOptions } from '../../interface/FetchOptions';

const markNotificationAsRead = () => async (): Promise<any> => {
  console.log('marking notification as read');
};

export default markNotificationAsRead;
